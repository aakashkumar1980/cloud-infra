# Test Configuration
# Inherits AWS config from application.yml (no duplication needed)

spring:
  application:
    name: company-backend-test

logging:
  level:
    root: INFO
    server: DEBUG
    client: DEBUG
    server.restapi_data_security: DEBUG
  pattern.console: "%d{HH:mm:ss.SSS} %-5level %c{2} - %m%n"
