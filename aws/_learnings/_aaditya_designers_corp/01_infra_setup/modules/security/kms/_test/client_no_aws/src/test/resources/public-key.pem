-----BEGIN PUBLIC KEY-----
PLACEHOLDER - REPLACE WITH ACTUAL PUBLIC KEY FROM AWS KMS

To get the actual public key, run the following command after terraform apply:

  aws kms get-public-key \
    --key-id alias/test_asymmetric_kms-us-east-1-dev-aaditya_designers_corp_v1-terraform \
    --region us-east-1 \
    --profile dev \
    --output text \
    --query PublicKey | base64 -d | openssl rsa -pubin -inform DER -outform PEM

Or download from AWS Console:
  1. Go to AWS KMS > Customer managed keys
  2. Find key with alias: test_asymmetric_kms-*
  3. Click on the key > Public key tab
  4. Download PEM file and replace this placeholder

-----END PUBLIC KEY-----
